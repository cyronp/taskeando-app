interface CreateCardProps{
    id: string;
    idColumn: string;
    name: string;
    description: string;
    date: string;
}

function CreateCard({id,idColumn,name,description,date}:CreateCardProps){
    
}